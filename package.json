{
  "name": "skywall",
  "private": true,
  "scripts": {
    "build": "tsc -b -i",
    "eslint": "eslint src/**/**.{ts,tsx}",
    "eslint:fix": "eslint src/**/**.{ts,tsx} --fix",
    "clean": "rimraf superbin **/bin **/tsconfig.tsbuildinfo",
    "test": "jest"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@cloudflare/workers-types": "^3.12.0",
    "@jest/types": "^28.1.1",
    "@types/node": "^18.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.1",
    "wrangler": "^2.0.12"
  },
  "dependencies": {
    "@skywall/service-cost-estimator": "*",
    "typescript": "^4.7.3"
  }
}
